services:
  # --- Backend Service (FastAPI) ---
  - type: web
    name: hackstorm-backend
    env: python
    rootDir: backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: LLM_PROVIDER
        value: cloud
      - key: LLM_MODEL
        value: llama-3.1-8b-instant
      - key: LLM_BASE_URL
        value: https://api.groq.com/openai/v1
      # The following should be set in the Render Dashboard for security
      # - LLM_API_KEY
      # - DATABASE_URL
      # - GEMINI_API_KEY
      # - SUPABASE_URL
      # - SUPABASE_SERVICE_ROLE_KEY

  # --- Frontend Service (Next.js) ---
  - type: web
    name: hackstorm-frontend
    env: node
    rootDir: frontend
    buildCommand: "npm install && npm run build"
    startCommand: "npm run start"
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://hackstorm-backend.onrender.com
      # The following should be set in the Render Dashboard
      # - NEXT_PUBLIC_SUPABASE_URL
      # - NEXT_PUBLIC_SUPABASE_ANON_KEY
